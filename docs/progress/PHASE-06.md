# Phase 06 progress - Face Detection, Recognition & People Intelligence

One line per task group, in the order section 8 asks for them. The exit report is
`docs/progress/PHASE-06-EXIT.md`.

| Task | Files touched | Tests added | Notes |
|---|---|---|---|
| SEC/SRC - schema and the sealed store, privacy tests first | `crates/aura-catalog/migrations/0006_people.sql`, `crates/aura-catalog/src/{migrate,lib}.rs`, `crates/aura-people/src/{vault,store,errors}.rs`, `crates/aura-app/src/biometric_keys.rs` | `crates/aura-people/tests/privacy.rs` (21) | Migration 6 adds seven tables and two views. `face_scan` is the resumability ledger phase 05 did not need. The envelope is BLAKE3 encrypt-then-MAC with a synthetic nonce; ADR-0013 section 4 records why, and why not `chacha20poly1305`. |
| SRML - the detector, multi-scale and tiled | `crates/aura-infer/src/onnx/fixtures.rs`, `crates/aura-vision/src/face/{detect,fixtures}.rs`, `xtask/src/models.rs`, `docs/model-cards/face_detect.md` | `crates/aura-vision/tests/face_geometry.rs` (28) | Letterbox rather than centre-crop, three strides from one pass, and faces and bodies from the same anchor - which is why phase 06 ships three models and not four. Recall 1.0000 against synthetic ground truth; the shipped weights find nothing (C1). |
| SRML - alignment and the recogniser | `crates/aura-vision/src/face/{align,embed}.rs`, `docs/model-cards/face_embed.md` | in `face_geometry.rs` | Closed-form Umeyama onto the published ArcFace layout; a similarity transform, never affine. The fp16 rounding is phase 05's `F16`, so one answer in the product to "what does 0.03125 become in sixteen bits". |
| MLL - the quality head and the voting gate | `crates/aura-vision/src/face/quality.rs`, `docs/model-cards/face_quality.md` | `crates/aura-vision/tests/face_quality.rs` (17) | A weighted geometric mean plus two hard cut-offs. `QUALITY_MODEL_WEIGHT` is 0.0 and that is a policy, not dead code (C2). Monotonicity is asserted in all three inputs. |
| MLR - clustering | `crates/aura-vision/src/face/cluster.rs`, `ml/models/face/tune_cluster.py` | `crates/aura-vision/tests/face_cluster.rs` (18) | Exact average linkage from running sums. The chain-merge guard is relative cohesion, not rank order, and ADR-0013 section 5 records why the phase document's mechanism refuses nothing. Siblings at 0.497 separation stay separate. |
| SRC - bodies and association | `crates/aura-vision/src/face/person.rs` | in `face_geometry.rs` | Anchor-shared pairs first, containment second, proximity last. `people_count` is a union, so a frame with bodies and no faces still counts people. |
| SRC - the graph, timelines and roles | `crates/aura-people/src/{graph,timeline}.rs`, `crates/aura-vision/src/face/roles.rs` | `crates/aura-vision/tests/face_roles.rs` (24) | Automation never assigns `bride` or `groom`. Confidence is capped at 0.62 while scenes are missing, and the reason string says so. |
| SRC - prominence and importance | `crates/aura-vision/src/face/prominence.rs`, `crates/aura-vision/config/prominence.toml`, `crates/aura-people/src/importance.rs` | in `face_roles.rs` | Weights in a versioned file, every score records the version, and scene tables are written now so phase 07 turns them on by supplying a label. |
| SFE/MFE - the People panel | `ui/src/components/people/{PeoplePanel,IdentityCard,MergeDialog}.tsx`, `ui/src/ipc/{types,client}.ts`, `crates/aura-app/src/{people_commands,contract/ipc,state}.rs` | `ui/src/components/people/PeoplePanel.test.tsx` (32) | Eleven commands; no template crosses the boundary. `chooseSurvivor` keeps the named identity, so a merge cannot discard a photographer's work. ADR-0014. |
| AGT - the couple hint | `crates/aura-cloud/src/couple_hint.rs`, `tests/cloud/cassettes/2*.json` | `crates/aura-cloud/tests/couple_hint.rs` (19) | Opaque handles only, so a model that describes a person fails validation rather than being stored. One cassette records exactly that answer. |
| QAL - gates, budgets and the eval harness | `crates/aura-cli/src/phase06.rs`, `crates/aura-perf/tests/people_budgets.rs`, `perf/budgets.toml`, `tests/eval/identity_eval.rs`, `justfile` | `identity_eval.rs` (9), `people_budgets.rs` (4) | `just phase-06-verify` runs thirteen checks and exits 0. The two GPU rows are waived with an expiry; the processor row replaces them. |
| DOC - cards, runbooks and the exit report | `docs/model-cards/face_*.md`, `docs/runbooks/AURA-{ML-5017..5021,SEC-9001..9005}.md`, `docs/adr/ADR-001{3,4}-*.md`, `docs/progress/PHASE-06*.md`, `CHANGELOG.md`, `CLAUDE.md`, `crates/aura-people/README.md` | - | Nine new codes, nine runbooks, three model cards with an honest fairness section, two ADRs. |
