{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local photo folder selection in the main editor window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
